# 运维机迁移验收 · 2026-07-28

> 对照 [docs/17-运维机迁移到服务器.md](../../docs/17-运维机迁移到服务器.md) **v1.3**  
> 目标机：`8.159.152.227` · MySQL 方案 **A**（dump → 本机 MariaDB）  
> 脚本：`scripts/ops_host_bootstrap.sh` · `scripts/ops_host_migrate_verify.py`

## 结果摘要

| 项 | 状态 |
|----|------|
| 项目路径 | `/opt/releasematch/releasematch/releasematch`（含 `.venv` / `.env` / `wrangler.toml`） |
| Git 根 | `/opt/releasematch/releasematch` |
| MariaDB | `10.11.x` · 库 `releasematch` 已导入；TCP root 密码与 `.env` 一致 |
| Ops systemd | `releasematch-ops.service` · `127.0.0.1:8090` |
| crontab | 测速 / 增量（**无** `--prepare-only`）/ TMDB |
| Cloudflare | 服务器本地 `CLOUDFLARE_API_TOKEN`；`wrangler whoami` OK；正式 upload-only 已验证 |
| Jackett | 现网 VPS IP 以 `servers.local.json` 为准；`accounts.base_url` 已对齐 |
| 本机 Mac | 无业务 cron · Ops 未监听 |

## 访问 Ops

```bash
ssh -L 8090:127.0.0.1:8090 root@8.159.152.227
# http://127.0.0.1:8090/  （RM_OPS_PASSWORD）
```

## 复验命令

```bash
ssh root@8.159.152.227
cd /opt/releasematch/releasematch/releasematch
.venv/bin/python scripts/ops_host_migrate_verify.py --expect-host 8.159.152.227
```

## 实迁文档改进（v1.3）

- 明确 GIT_ROOT vs PROJECT_ROOT 三层路径  
- Node 22 + wrangler 4；CF Token 勿 push  
- MariaDB TCP 密码；Jackett accounts↔servers；首传防稀疏 dist  
- bootstrap / verify 脚本落地  

## 待人工闭环（若仍开放）

```
[ ] 若历史 Jackett IP 曾不可达：在新 VPS 上确认 crontab 仅 healthcheck
[ ] （可选）SOCKS：按需 start_ssh_socks_tunnel.sh
```
