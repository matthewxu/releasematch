# 今日验收清单

> **日期：** 2026-06-30（收工更新：2026-07-01）  
> **轨道：** T0 数据管道 MVP ✅ → T1 IG 引擎起步 ✅  
> **今日目标：** 环境就绪 → 四源 PoC → client MVP → **T0 终验 + T1 起步 + VPS 基准测试**  
> **今日不碰：** T2 测速、T3 生成器批量、C0 CF 部署、GSC

---

## 一、今日 Todo（按执行顺序，细化到功能）

### 块 A — 环境与配置（约 30~45 分钟）

| # | 功能 | 具体操作 | 产出文件/命令 |
|---|------|----------|---------------|
| A1 | Python 虚拟环境 | `cd releasematch` → `python -m venv .venv` → 激活 → `pip install -r requirements.txt` | `.venv/` |
| A2 | Jackett 配置 | 复制 `accounts.example.json` → `accounts.local.json`，填入真实 `api_key` | `workflow/torrent_sources/accounts.local.json` |
| A3 | 环境变量（可选） | 设置 `JACKETT_API_KEY`；确认 `RM_TMDB_DATA_MODE=standalone` | 终端 / `.env`（若使用） |
| A4 | Jackett 服务 | Docker 启动 Jackett，`http://127.0.0.1:9117` 可访问 Dashboard | 本地 Jackett 实例 |
| A5 | 工作流状态基线 | `python -m workflow.run status` | 终端 JSON 快照（留作对比） |

---

### 块 B — T0-4 四源 PoC 验证（约 30 分钟）

| # | 功能 | 具体操作 | 通过含义 |
|---|------|----------|----------|
| B1 | Jackett Torznab | 跑 PoC `[1/4]`：BB S04E06，`tvdbid=81189` | `OK status=200`，非 SKIPPED |
| B2 | EZTV JSON | PoC `[2/4]`：`imdb_id=904747` | `OK status=200` |
| B3 | YTS API | PoC `[3/4]`：Matrix `tt0133093` | `OK status=200` |
| B4 | Nyaa RSS | PoC `[4/4]`：Breaking Bad RSS | `OK status=200`（T0 不实现 client，仅连通） |
| B5 | PoC 结果归档 | 将四源 OK/FAIL 写入今日工作日志 | `worklogs/2026-06-30/` |

```powershell
cd releasematch
.\scripts\poc_phase0.ps1
# 或带 Key：
$env:JACKETT_API_KEY = "你的KEY"
.\scripts\poc_phase0.ps1
```

---

### 块 C — T0-1 元数据（查漏补缺，约 15 分钟）

| # | 功能 | 具体操作 | 验收点 |
|---|------|----------|--------|
| C1 | standalone 映射 | 确认 `1396` → `imdb=tt0903747`、`tvdb=81189` | `resolve_external_ids` 返回正确 |
| C2 | CLI 透传 | `torrent_sources.run test` 能拿到 `external_ids` | JSON 中 `source` 非 `standalone_missing` |
| C3 | MySQL 模式（可选） | 若有字幕库：设 `RM_TMDB_DATA_MODE=mysql` 测一条 | 有则 `source=mysql`，无则跳过 |

```powershell
python -c "from workflow.metadata.external_ids import resolve_external_ids; import json; print(json.dumps(resolve_external_ids(1396,'tv'), indent=2))"
```

---

### 块 D — T0-2a `jackett_client.py`（今日核心，约 3~4 小时）

| # | 功能 | 子功能 | 说明 |
|---|------|--------|------|
| D1 | 类骨架 | `JackettClient.__init__` | 读 `load_accounts_config()`：`base_url`、`api_key` |
| D2 | 剧集搜索 | `search_tv(tvdb_id, season, episode)` | Torznab `t=tvsearch` |
| D3 | 电影搜索 | `search_movie(imdb_id)` | Torznab `t=movie` 或 `t=search` |
| D4 | HTTP 请求 | `_search(indexer, params)` | `requests.get`，超时 30s，失败返回 `[]` |
| D5 | XML 解析 | `_parse_torznab_xml(xml, indexer)` | 解析 `item`：`title`、`size`、`seeders`、`infoHash`、`magneturi` |
| D6 | 字段映射 | `ResourceItem` 填充 | `infohash` 小写 40 位、`indexer=jackett:{name}`、`magnet_uri` 非空 |
| D7 | 单测槽位 | BB S04E06 手动调用 | `tvdb=81189, s=4, e=6` → `len(items) >= 1` |

**参考规格：** `docs/02-数据源技术方案-详细展开.md` §3.3 Torznab

---

### 块 E — T0-2b `eztv_client.py`（时间充裕再做，约 2 小时）

| # | 功能 | 子功能 | 说明 |
|---|------|--------|------|
| E1 | IMDb 归一化 | `imdb_id` → 数字 `904747` | 去 `tt` 前缀 |
| E2 | 列表拉取 | `GET /api/get-torrents?imdb_id=&limit=100` | 分页 `page=1` |
| E3 | 季集过滤 | `season` + `episode` 匹配 | 只保留目标集 |
| E4 | 字段映射 | `title`、`torrent_url`/`magnet` → `ResourceItem` | `indexer=eztv` |
| E5 | 单测 | BB S04E06 | `len(items) >= 1` |

---

### 块 F — T0-2d `fetch_service.py`（今日可只做骨架）

| # | 功能 | 子功能 | 说明 |
|---|------|--------|------|
| F1 | 编排入口 | `fetch_slot(request: FetchRequest)` | 按 `media_type` 分支 |
| F2 | 多源并发/串行 | 调 Jackett + EZTV（+ YTS 电影） | T0 先串行 + `min_interval_sec` |
| F3 | infohash 去重 | 同 hash 合并，保留 seeders 较高者 | 为 T1 跨源打基础 |
| F4 | 接 `run.py test` | 替换占位 `items=[]` | `cmd_test` 调 `fetch_service` |
| F5 | 缓存写入 | 成功结果 `cache_index.upsert` | `cache_key` = `tv:1396:s04e06` |

> **说明：** `release_parser.py`（T0-3）与 `yts_client.py` 可排到明日；今日以 Jackett + fetch 骨架打通为主。

---

### 块 G — 文档与日志（日终 15 分钟）

| # | 功能 | 操作 |
|---|------|------|
| G1 | 更新工作日志 | 记录 PoC 结果、已完成 client、阻塞项 |
| G2 | 勾选明日计划 | jackett 未完成项 / eztv / yts / release_parser |

---

## 二、验收清单（功能级）

### 日终必达（Minimum — 今日收工标准）

| ID | 功能模块 | 验收项 | 验收命令 / 判定 |
|----|----------|--------|-----------------|
| D-A1 | 环境 | `requests`、`PyYAML` 可 import | `python -c "import requests, yaml"` 无报错 |
| D-A2 | 配置 | `accounts.local.json` 存在且 `has_api_key=true` | `python -m workflow.torrent_sources.run status` |
| D-A3 | PoC Jackett | 非 SKIPPED，HTTP 200 | `poc_phase0.ps1` `[1/4] OK` |
| D-A4 | PoC EZTV | HTTP 200 | `[2/4] OK` |
| D-A5 | PoC YTS | HTTP 200 | `[3/4] OK` |
| D-A6 | PoC Nyaa | HTTP 200 | `[4/4] OK` |
| D-A7 | external_ids | BB 槽位 ID 完整 | `imdb_id=tt0903747`, `tvdb_id=81189` |
| D-A8 | 工作日志 | 今日 PoC 结果已记录 | `worklogs/2026-06-30/` 有更新 |

**日终判定：** 上表 **8/8 全绿** = 今日合格（即使 client 未写完）。

---

### 日终理想（Target — 今日冲刺目标）

| ID | 功能模块 | 验收项 | 判定标准 |
|----|----------|--------|----------|
| D-B1 | `jackett_client` | `search_tv` 返回列表 | `len(items) >= 1` |
| D-B2 | `jackett_client` | 单条 `ResourceItem` 字段完整 | 见下方字段表 |
| D-B3 | `jackett_client` | `infohash` 格式 | 40 位小写十六进制 |
| D-B4 | `jackett_client` | `magnet_uri` | 以 `magnet:?` 开头且含 `xt=urn:btih:` |
| D-B5 | `jackett_client` | `seeders` | `>= 0` 整数（可为 0） |
| D-B6 | `jackett_client` | `title_raw` | 非空，含 S04E06 或剧集标识 |
| D-B7 | CLI 集成 | `torrent_sources.run test` | 经 Jackett 路径 `items` 非空（可仅 Jackett 单源） |

```powershell
python -m workflow.torrent_sources.run test --tmdb 1396 --season 4 --episode 6
# 期望：items 非空，error 为 null
```

**理想判定：** **D-B1~B7 全绿**。

---

### T0 阶段终验（本周目标 — **2026-06-30 已通过**）

| ID | 功能模块 | 验收项 | 判定标准 |
|----|----------|--------|----------|
| T0-F1 | 三源 MVP | Jackett + EZTV + YTS 均有数据 | 单槽 `count >= 2`（跨源去重后） |
| T0-F2 | `release_parser` | 从 `title_raw` 解析 | `release_group`、`resolution`、`codec`、`source` 非全空 |
| T0-F3 | 缓存 | 二次请求命中缓存 | `cached: true`，`cache_entries` 增加 |
| T0-F4 | 总控 CLI | `workflow.run run 4c --test` | 退出码 `0`，`items >= 2` |
| T0-F5 | 电影槽位 | Matrix `tmdb=603` | `movie:603` 至少 1 条 YTS/Jackett |
| T0-F6 | 薄页门禁数据 | 槽位可供 C1 | `count >= 2`（与 SEO 门禁一致） |
| T0-F7 | 模块状态 | `workflow.run status` | `torrent_sources` 从 `scaffold` → `mvp` |

---

## 三、`ResourceItem` 单条字段验收表

| 字段 | 必填 | 验收规则 |
|------|------|----------|
| `infohash` | ✅ | 40 字符，小写，`[0-9a-f]` |
| `title_raw` | ✅ | 非空字符串 |
| `magnet_uri` | ✅ | `magnet:?xt=urn:btih:{infohash}` 形式 |
| `indexer` | ✅ | 如 `jackett:all`、`eztv`、`yts` |
| `seeders` | ✅ | `int >= 0` |
| `release_group` | 🔶 | T0 可先空；T0-3 parser 后应有值 |
| `source` | 🔶 | 如 `WEB-DL`、`BluRay`；parser 后填充 |
| `resolution` | 🔶 | 如 `1080p`、`720p` |
| `codec` | 🔶 | 如 `H.264`、`HEVC` |
| `size_bytes` | 🔶 | `> 0`（源有则填） |
| `peers` | ⬜ | 可选 |
| `cross_source_count` | ⬜ | T0 默认 `1`；T1 跨源后 `>1` |

---

## 四、今日时间分配建议

```
09:00–10:00   块 A 环境 + 块 B PoC + 块 C external_ids
10:00–13:00   块 D jackett_client（D1–D7）
14:00–16:00   块 F fetch_service 骨架 + CLI 集成
16:00–17:00   块 E eztv（可选）或补 jackett 边界情况
17:00–17:15   块 G 工作日志 + 对照日终验收表勾选
```

---

## 五、阻塞与降级策略

| 阻塞 | 降级动作 | 仍须完成 |
|------|----------|----------|
| 无 Jackett / 无 Key | PoC `[1/4]` 记 FAIL；先写 client + mock 单测 | B2~B4、C1、G1 |
| Jackett 有结果但 EZTV 失败 | 今日只验 Jackett 路径 | D-B1~B7 |
| 四源网络全 FAIL | 记录错误码；用 `docs/02` §十二 curl 手工验证 | G1 + 明日重试 |

---

## 六、快速勾选表

```
【日终必达 — Day 1 上午】
[x] D-A1  venv + pip install
[x] D-A2  accounts.local.json
[x] D-A3  PoC Jackett OK（非 SKIPPED）
[x] D-A4  PoC EZTV OK
[x] D-A5  PoC YTS OK
[~] D-A6  PoC Nyaa 直连超时（T0 不阻塞；T1 经 SSH SOCKS 已通，见 §八）
[x] D-A7  external_ids BB 完整
[x] D-A8  工作日志已更新

【日终理想 — Day 1 上午】
[x] D-B1  search_tv >= 1 条（晚间：日本 VPS Jackett + thepiratebay/nyaasi）
[x] D-B2  ResourceItem 必填字段齐全
[x] D-B3  infohash 40 位小写
[x] D-B4  magnet_uri 合法
[x] D-B5~B6 seeders / title_raw
[x] D-B7  torrent_sources.run test 非空

【T0 阶段终验 — 同日完成】
[x] T0-F1  三源 + count >= 2（EZTV+YTS 直连；晚间 Jackett VPS 补充）
[x] T0-F2  release_parser
[x] T0-F3  缓存命中
[x] T0-F4  workflow.run 4c --test 退出码 0
[x] T0-F5  电影 Matrix / Inception 槽位
[x] T0-F6  薄页门禁数据充足（12~20 条/集）
[x] T0-F7  status → mvp

【T1 起步 — 同日晚间扩展】
[x] T1-G1  groups.yaml 98 组 + groups_registry.py
[x] T1-G2  评分 tie-break（seeders → 1080p → 跨源 → tier）
[x] T1-G3  cross_source_count 跨源聚合
[x] T1-G4  nyaa_client.py（RSS 多镜像 + SSH SOCKS 回退）
[x] T1-G5  batch_fetch.py 批量槽位
[x] T1-G6  score_slot / pipeline --fetch 接真实 FetchService
[x] T1-G7  日本 VPS Jackett（104.105.140.11）+ accounts.local.json
[x] T1-G8  7 槽 pipeline 基准测试 7/7 成功（见 §八）

【待续 — 2026-07-01 起】
[ ] T2-S1  speedtest phase1_connectivity
[ ] T3-G1  generate_one.py / generate_batch.py
[ ] C0-D1  CF Pages 部署
[ ] OPS-1   PoC Nyaa 直连 OK（可选，非阻塞）
```

---

## 七、执行记录（当日填写）

| 时间 | 块 | 结果 | 备注 |
|------|-----|------|------|
| 2026-06-30 上午 | A | ✅ 通过 | 见 `块A-环境配置.md` |
| 2026-06-30 上午 | B | ✅ T0 通过 3/4 | 见 `块B-PoC四源验证.md`；Nyaa PoC 直连超时 |
| 2026-06-30 上午 | C | ✅ 通过 | standalone 映射 + CLI 透传正常 |
| 2026-06-30 上午 | D/E/F | ✅ T0 MVP | 见 `块D-T0数据管道client.md`；EZTV+YTS+fetch+parser |
| 2026-06-30 下午 | T0 终验 | ✅ F1~F7 | 见 `T0收尾与T1起步.md` |
| 2026-06-30 下午 | T1 起步 | ✅ G1~G6 | groups、跨源、nyaa_client、batch_fetch、scorer 真实数据 |
| 2026-06-30 晚间 | VPS + 代理 | ✅ 完成 | 日本 Jackett `104.105.140.11`；SSH SOCKS 回退；见 `jackett-japan-vps-config.md` |
| 2026-06-30 晚间 | Pipeline 基准 | ✅ 7/7 | 7 槽冷拉取 507s（-37%）；见 `槽位-pipeline-基准测试.md` |
| 2026-06-30 | G | ✅ 已更新 | 本文件 + 各块日志 |

**日终结论：** ☑ 必达通过（8/8，D-A6 按 T0 降级计）　☑ 理想通过（D-B1~B7 全绿，含 VPS Jackett）　☑ T0 终验通过　☑ T1 起步完成

**块 A 勾选（2026-06-30）：**

```
【日终必达 - 块 A 相关】
[x] D-A1  venv + pip install（requests/PyYAML/PyMySQL 已装）
[x] D-A2  accounts.local.json 已创建，has_valid_api_key=true
[x] D-A3  PoC Jackett — API Key 有效，Torznab HTTP 200
[x] D-A4  Jackett 服务 — jackett_probe.reachable=true, status=200
[x] D-A5  workflow.run status 基线已可运行
```

---

## 八、第二轮扩展验收（2026-06-30 晚间）

> **背景：** 本机 Jackett indexer 剧集 0 条 → 部署日本 VPS + 移除 1337x/FlareSolverr 慢路径 + Nyaa SSH SOCKS 回退。

### 8.1 海外 Jackett

| ID | 验收项 | 判定 | 状态 |
|----|--------|------|------|
| VPS-J1 | `accounts.local.json` 指向 `104.105.140.11:9117` | `jackett_probe.reachable=true` | ✅ |
| VPS-J2 | 剧集 indexer | thepiratebay + nyaasi（已移除 1337x / torrentgalaxyclone） | ✅ |
| VPS-J3 | FlareSolverr | 已从慢路径移除 | ✅ |

### 8.2 Nyaa / 日韩源

| ID | 验收项 | 判定 | 状态 |
|----|--------|------|------|
| NYAA-N1 | `nyaa_client.py` + `nyaa_live_action_client.py` | 模块存在且 pipeline 可调用 | ✅ |
| NYAA-N2 | SSH SOCKS 回退 | `TORRENT_PROXY=socks5h://127.0.0.1:1080` 可拉取 | ✅ |
| NYAA-N3 | PoC `[4/4]` 直连 | HTTP 200 | ⚠️ 仍超时（pipeline 路径已通，非阻塞） |

### 8.3 Pipeline 基准测试（7 槽）

| ID | 验收项 | 判定 | 状态 |
|----|--------|------|------|
| PIPE-P1 | 7 槽全部成功 | 0 失败 | ✅ |
| PIPE-P2 | 剧集单槽耗时 | ~70~92s（首槽 122s） | ✅ |
| PIPE-P3 | 电影 Inception | 4.5s，3 magnets | ✅ |
| PIPE-P4 | 条数 / 跨源 | 12~20 条/集，跨源 1~2/3 | ✅ |
| PIPE-P5 | 总 wall 时间 | 507s（较上轮 -37%） | ✅ |

```bash
# 复现
bash scripts/start_ssh_socks_tunnel.sh
export TORRENT_PROXY=socks5h://127.0.0.1:1080
python scripts/benchmark_slots_pipeline.py --force
```

### 8.4 模块状态快照（2026-06-30 收工）

| 模块 | 状态 | 说明 |
|------|------|------|
| `torrent_sources` | **mvp** | 四源 client + fetch + cache |
| `recommended` | **active** | score_slot 真实数据 |
| `storage_mysql` | **active** | 89 download_resources，10 media_pages |
| `portal_generator` | scaffold | 待 T3 generate_* |
| `metadata` / `priority` | scaffold | 未启动 |

---

## 九、明日计划（2026-07-01）

| 优先级 | 任务 | 关联轨道 |
|--------|------|----------|
| P1 | 槽位 pipeline 稳定性复测（无 `--force` 缓存路径） | T1 |
| P1 | `generate_one.py` 骨架 + 单页 HTML 输出 | T3 |
| P2 | speedtest phase1 连通性探测 | T2 |
| P2 | CF Pages 部署 Trust 四页 | C0 |
| P3 | 扩展 groups.yaml / 评分规则微调 | T1 |
