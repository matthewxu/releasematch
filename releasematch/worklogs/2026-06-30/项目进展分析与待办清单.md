# 项目进展分析与待办清单

> **日期：** 2026-06-30  
> **关联轨道：** T0（当前）、C0（部分超前）  
> **状态快照：** 脚手架阶段，整体约 20%~25%

---

## 一、当前位置

| 轨道 | 计划周期 | 完成度 | 阻塞点 |
|------|----------|--------|--------|
| **T0** 数据管道 | W1~2 | ~25% | 四源 client + fetch_service 未实现 |
| **T1** IG 引擎 | W3~5 | ~10% | 依赖 T0 真实 items |
| **T2** 测速 | W6 | 0% | `speedtest/` 未建 |
| **T3** 生成器 + D1 | W7 | ~15% | 仅有 Jinja2 模板，无 `generate_*.py` |
| **C0** Trust 壳 | W7~8 | ~70% | 静态页就绪，缺 CF Pages 部署 |
| **C1~C4** 内容扩展 | W8+ | 0% | 正确阻塞于 T3 |

**T0 验收标准（未达成）：**

```bash
python -m workflow.torrent_sources.run test --tmdb 1396 --season 4 --episode 6
# 期望：返回真实 magnet items（≥2）
# 现状：items 为空，clients 尚未实现
```

---

## 二、已完成清单

### 文档与架构

- 双轨路线图 `docs/04` v1.2 生效
- 独立目录结构、`requirements.txt`、`.gitignore`

### 工作流（骨架）

- `workflow/run.py`：`status`、`run 4c/recommended --test`
- `external_ids.py`：Demo 映射（Breaking Bad / Matrix / Inception）+ MySQL 只读
- `torrent_sources`：models、SQLite cache_index、config
- `scorer.py`：5 组 Demo + 规则评分
- `queue_builder.py`：BB S4 全季 + S1 前 5 集 + 2 电影

### Portal（设计超前）

- `design-system.css`、Trust 四页、404、robots.txt
- 演示页：`breaking-bad/s4e6`、`inception-2010`（magnet 占位）
- `generator/templates/` Jinja2 骨架

### 缺失（glob 验证为 0）

- 所有 `*_client.py`
- `fetch_service.py`、`release_parser.py`
- `generate_one.py` / `generate_batch.py`
- `torrent_sources/speedtest/`

---

## 三、后续待办（按优先级）

### P0 — T0 数据管道 MVP（当前冲刺）

| ID | 任务 | 估时 |
|----|------|------|
| T0-4 | `poc_phase0.ps1` 四源全绿 | 0.5d |
| T0-2a | `jackett_client.py` | 2d |
| T0-2b | `eztv_client.py` | 1d |
| T0-2c | `yts_client.py` | 1d |
| T0-2d | `fetch_service.py` | 1d |
| T0-3 | `release_parser.py` 基础 | 1~2d |
| — | 验收 `workflow.run run 4c --test` | 0.5d |

### P1 — T1 IG 引擎（T0 后）

- scorer 接真实 items
- `groups.yaml`（~100 组）
- 编码参数分析 Phase 1
- 跨源交叉验证 + `nyaa_client` + `batch_fetch`

### P2 — T2 测速（W6）

- `speedtest/phase1_connectivity.py`
- D1 测速 API、seeders cron

### P3 — T3 发布链（阻塞 C1）

- D1 sync Worker
- `generate_one.py` / `generate_batch.py`
- 薄页门禁（magnet < 2 不 index）

### P4 — C0 收尾（可与 T3 并行）

- CF Pages 部署
- Trust 页法律审核
- **不提交 GSC**

### 暂缓

- C1 验证集 20 页、C2 GSC、T4 扩展、T5 深度增强

---

## 四、建议执行顺序

```
本周   poc 全绿 → jackett/eztv/yts → fetch_service → release_parser → T0 验收
下周   scorer 真实数据 + groups.yaml + 跨源 + 编码 P1
W6~7   T2 测速 → T3 生成器 + D1（C0 CF 并行）
W8~9   C1 验证集 → C2 GSC
```

---

## 五、风险

1. Portal 设计稿超前于数据管道，避免手工填 magnet 冒充进度
2. Jackett 需本地实例 + API Key
3. T3 完成前禁止 GSC 与批量上页

---

## 六、明日计划

- [ ] 配置 `accounts.local.json` / `JACKETT_API_KEY`
- [ ] 运行 `poc_phase0.ps1` 记录四源结果
- [ ] 启动 `jackett_client.py` 实现
