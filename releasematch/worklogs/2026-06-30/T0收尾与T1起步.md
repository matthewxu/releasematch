# T0 收尾与 T1 起步

> **日期：** 2026-06-30  
> **状态：** T0 阶段终验通过；T1 `score_slot` 已接真实数据

---

## 本次完成

| 项 | 说明 |
|----|------|
| `workflow.run run 4c --force` | 总控 CLI 透传 `--force` 至 torrent 拉取 |
| `score_slot()` | `recommended/scorer.py` 调用 `FetchService` 真实 items |
| `workflow.run run recommended` | 默认走真实拉取；`--demo` 保留 Demo 模式 |
| `pipeline --fetch` | 接入 `FetchService`，失败时回退 demo |
| T0 终验 | F1~F7 全部通过（Nyaa/Jackett 剧集除外） |

---

## 验收命令

```bash
# T0 总控（剧集 + 电影）
python -m workflow.run run 4c --test --tmdb 1396 --season 4 --episode 6 --force
python -m workflow.run run 4c --test --tmdb 603 --media-type movie --force

# 缓存命中（无需 --force）
python -m workflow.torrent_sources.run test --tmdb 1396 --season 4 --episode 6
# → cached: true

# T1 评分（真实数据）
python -m workflow.run run recommended --tmdb 1396 --season 4 --episode 6
python -m workflow.run run recommended --tmdb 603 --media-type movie
```

---

## 已知限制

1. **Jackett 本机剧集 indexer** 仍 0 条 → 剧集依赖 EZTV 直连
2. **Nyaa** 直连超时 → T1 再实现 `nyaa_client.py`
3. ~~**评分同分** 时按列表顺序取首条~~ → 已用 seeders / 1080p / 跨源 / tier tie-break
4. ~~**PLUTONiUM 等组** 未入 groups.yaml~~ → `data/groups.yaml` 98 组 + `groups_registry.py`

---

## 下一步（T1）

- [x] `groups.yaml` 扩展 100+ 压制组（98 组）
- [x] 评分同分 tie-break（seeders → 1080p → 跨源 → tier）
- [ ] 跨源 `cross_source_count` 聚合逻辑
- [ ] `nyaa_client.py`（需海外网络或 Jackett）
- [ ] `batch_fetch.py` 批量槽位
- [ ] 海外 Linode Jackett + 更新 `accounts.local.json` base_url
