{
  "_comment": "与 linode_vps.py 同目录。复制为 linode.local.json 并填入真实 token / 密码。linode.local.json 已在 .gitignore，勿提交 GitHub。",
  "token": "YOUR_LINODE_PERSONAL_ACCESS_TOKEN",
  "http_timeout": null,
  "defaults": {
    "_comment": "create / 一键开通未传 CLI 时使用。label 为实例名；ssh.password 建议与 servers.local.json → jackett_vps_japan.ssh.password 一致。",
    "label": "rm-jackett-jp",
    "region": "jp-osa",
    "type": "g6-nanode-1",
    "image": "linode/debian12",
    "ssh": {
      "user": "root",
      "password": "YOUR_SSH_PASSWORD",
      "port": 22
    }
  }
}
