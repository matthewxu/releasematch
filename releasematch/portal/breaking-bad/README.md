# breaking-bad — 生成器输出路径

本目录 **不再存放手写演示 HTML**。单集与 Hub 页由 `python -m workflow.run generate all` 写入 `portal/dist/breaking-bad/`。

本地预览：`python -m workflow.run serve`（读 MySQL）或 `generate all` 后查看 `portal/dist/`。
